# === 必填 ===
# DeepSeek API Key (在 platform.deepseek.com 获取)
DEEPSEEK_API_KEY=sk-your-deepseek-key

# GitHub Copilot 认证使用设备 OAuth Token，无需在此配置
# 运行 podman compose up -d 之前，先完成下面第3步的设备登录
# 登录后 token 会自动写入 copilot-anthropic-proxy/github_token
