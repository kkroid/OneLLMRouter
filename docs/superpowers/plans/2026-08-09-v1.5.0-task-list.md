# OneLLMRouter v1.5.0 Execution Task List

> Product design: `docs/superpowers/specs/2026-08-09-v1.5.0-plan.md`
>
> Approved baseline: `d9c5db1fb21eb8f1b2a9f05ec75728744f25df6d`
>
> Execution model: one isolated worktree and one stable Implementation Worker per Task.

## Contract Rules

- The approved product design is authoritative. A Task may not reinterpret product behavior.
- Each Task owns only its declared write scope. Reading other files is allowed; writing outside scope is not.
- A Worker must commit its implementation and report the full HEAD plus the listed verification evidence.
- A missing or contradictory decision is `design_gap`; the Worker must not resolve it by expanding scope.
- Tests use `httptest`, temporary directories, or dynamic loopback ports. They must not contact or stop the active Router.
- No Task may add Auto Failover, MCP, Skill, or Prompt management.
- Integration is serial even when implementation Tasks are independent.

## Dependency Overview

```text
T1 Usage contract
  -> T2 Usage storage
      -> T3 Protocol collection
          -> T4 Stats CLI
              -> T9 Usage UI

T5 Go platform boundary
  -> T7 Core config commands
      -> T8 Providers/Models UI

T6 Qt platform boundary
  -> T8 Providers/Models UI
  -> T9 Usage UI

T1..T9 -> T10 Release and Retry regression
```

`T5` and `T6` may run independently of the Usage chain after their scopes are checked against any active Task.

## T1: Usage Data Contract

**Goal:** Add the protocol-neutral Usage record and preserve the distinction between an absent token field and an explicit zero.

**Dependencies:** None.

**Write scope:**

- `internal/usage/types.go`
- `internal/usage/types_test.go`
- `internal/translate/core_types.go`
- `internal/translate/core_adapter_test.go`
- `internal/translate/core_response.go`
- `internal/translate/types.go`

**Required contract:**

- Define the approved fields: time, request ID, Provider, requested/upstream model, protocol, five nullable token fields, source, attempt, and status.
- Token fields must represent `null`, `0`, and positive values distinctly.
- Protocol/source/status values must use closed constants rather than free-form call-site strings.
- Existing Anthropic/OpenAI translation behavior must remain compatible.

**Non-goals:** No file I/O, aggregation, CLI, proxy collection, or Qt changes.

**Acceptance:**

- JSON marshaling emits `null` for absent token values and `0` for explicit zero.
- Input/output/cache read/cache write/reasoning fields round-trip without loss.
- Existing translator tests remain green.

**Verification:**

```text
go test -count=1 ./internal/usage ./internal/translate
go vet ./internal/usage ./internal/translate
git diff --check
```

## T2: Usage JSONL Storage And Attempt Identity

**Goal:** Provide UTC daily JSONL storage and stable attempt identity for the protocol collectors added by T3.

**Dependencies:** T1.

**Write scope:**

- `internal/usage/store.go`
- `internal/usage/store_test.go`
- `internal/usage/reader.go`
- `internal/usage/reader_test.go`
- `internal/upstream/retry.go`
- `internal/upstream/retry_test.go`

**Required contract:**

- Store complete records under `~/.onellm/usage/YYYY-MM-DD.jsonl`, with an injectable base directory and clock for tests.
- Expose the same `request_id` across Retry and a one-based `upstream_attempt` to later collectors.
- Reject or ignore a duplicate write for the same `request_id + upstream_attempt` within one process.
- A malformed line is reported by the reader and does not prevent later valid lines from being read.
- Persistence errors are logged with no secret/body data and do not replace a successful model response.

**Non-goals:** No production-path record emission, protocol parsing, stats aggregation, retention deletion, or UI.

**Acceptance:**

- UTC day rollover, append behavior, duplicate-attempt protection, malformed lines, and write failures are covered by temporary-directory tests.
- Stored files contain no API key, Authorization header, request body, or response body.
- Retry exposes stable attempt identity without changing timing or response behavior.

**Verification:**

```text
go test -count=1 ./internal/usage ./internal/upstream
go test -race -count=1 ./internal/usage ./internal/upstream
git diff --check
```

## T3: Usage Collection Across Protocol Paths

**Goal:** Collect explicit upstream Usage for Anthropic Messages, OpenAI Chat Completions, and OpenAI Responses across stream/non-stream and direct/translated paths.

**Dependencies:** T2.

**Write scope:**

- `internal/proxy/handler.go`
- `internal/proxy/proxy_test.go`
- `internal/proxy/proxy_openai_test.go`
- `internal/proxy/proxy_responses_test.go`
- `internal/proxy/responses_sse.go`
- `internal/upstream/retry.go`
- `internal/upstream/retry_test.go`
- `internal/translate/core_response.go`
- `internal/translate/stream.go`
- `internal/translate/types.go`
- `internal/translate/core_adapter_test.go`
- `internal/usage/collector.go`
- `internal/usage/collector_test.go`
- `cmd/onellm-router/main.go`

**Required contract:**

- Capture only fields explicitly returned by the upstream protocol; never estimate with a local tokenizer.
- Preserve upstream Usage fields even when the client-facing protocol exposes fewer fields.
- Record Retry attempts independently and never duplicate the final successful attempt.
- The Retry attempt observer is observation-only and must not change Retry decisions, timing, or failures.
- A stream without a terminal Usage event keeps token fields unknown.
- A response with no explicit token field keeps `usage_source` and all token fields unknown.
- Persistence failures are logged safely and never replace or alter a successful model response.

**Non-goals:** No aggregation, CLI, UI, or Retry-policy change.

**Acceptance:**

- A test matrix covers all three protocols, stream/non-stream, and direct/translated paths that exist in the Router.
- Missing and explicit-zero Usage remain distinct through capture and persistence.
- Retry/stream tests prove each upstream attempt has one record owner: the observer owns failed and buffered attempts, while the stream collector owns the accepted streaming attempt.
- A persistence failure test proves the original successful response remains unchanged.
- Stream byte/event order and failure behavior remain unchanged.

**Verification:**

```text
go test -count=1 ./internal/proxy ./internal/translate ./internal/upstream ./internal/usage ./cmd/onellm-router
go test -race -count=1 ./internal/proxy ./internal/upstream ./internal/usage
git diff --check
```

## T4: Daily, Weekly, And Monthly Stats CLI

**Goal:** Aggregate Usage by Provider and model for UTC day, ISO week, and month, with stable table and JSON output.

**Dependencies:** T3.

**Write scope:**

- `internal/usage/stats.go`
- `internal/usage/stats_test.go`
- `cmd/onellm-router/stats.go`
- `cmd/onellm-router/stats_test.go`
- `cmd/onellm-router/main.go`

**Required contract:**

- Commands: `stats day [YYYY-MM-DD]`, `stats week [YYYY-Www]`, and `stats month [YYYY-MM]`, each with `--json`.
- Group by Provider, requested model, and upstream model.
- Return token totals, request outcomes, Retry counts, and unknown counts.
- Distinguish all reported attempt Usage from final successful-request Usage.
- Empty data is a successful empty result; malformed lines are counted and skipped.

**Non-goals:** No HTTP API, charts, cost estimation, retention deletion, or Qt changes.

**Acceptance:**

- UTC day and ISO week/year boundaries have deterministic tests.
- JSON and table output are derived from the same aggregation result.
- Re-reading the same input does not change totals.

**Verification:**

```text
go test -count=1 ./internal/usage ./cmd/onellm-router
go vet ./internal/usage ./cmd/onellm-router
git diff --check
```

## T5: Go Platform Boundary

**Goal:** Remove unconditional Windows dependencies from the Go command package while preserving Windows behavior.

**Dependencies:** None.

**Write scope:**

- `cmd/onellm-router/main.go`
- `cmd/onellm-router/install_test.go`
- `cmd/onellm-router/platform.go`
- `cmd/onellm-router/platform_windows.go`
- `cmd/onellm-router/platform_windows_test.go`
- `cmd/onellm-router/platform_unix.go`
- `cmd/onellm-router/platform_unix_test.go`
- `.github/workflows/release.yml`

**Required contract:**

- Registry, console detachment, executable naming, and install/autostart behavior live behind platform files and build tags.
- Non-Windows builds return explicit unsupported behavior for Windows-only lifecycle commands.
- Shared files do not directly import `golang.org/x/sys/windows/registry` or call `kernel32.dll`.
- Windows install and tray-child behavior remain unchanged.

**Non-goals:** No LaunchAgent, systemd/XDG autostart, packaging, or Qt changes.

**Acceptance:**

- Go build/test succeeds for Windows, Linux, and macOS targets supported by CI.
- Existing Windows installation tests remain green.
- A source scan confirms Windows APIs are confined to Windows adapter files.

**Verification:**

```text
go test -count=1 ./cmd/onellm-router
go vet ./cmd/onellm-router
pwsh -NoProfile -Command '$env:CGO_ENABLED="0"; $env:GOOS="linux"; go build -o "$env:TEMP/onellm-router-linux" ./cmd/onellm-router'
pwsh -NoProfile -Command '$env:CGO_ENABLED="0"; $env:GOOS="darwin"; go build -o "$env:TEMP/onellm-router-darwin" ./cmd/onellm-router'
git diff --check
```

## T6: Qt Platform Boundary

**Goal:** Centralize Qt platform-specific paths, process flags, autostart, restart integration, and executable naming.

**Dependencies:** None.

**Write scope:**

- `desktop/CMakeLists.txt`
- `desktop/src/platform`
- `desktop/src/router_discovery.cpp`
- `desktop/src/router_process.cpp`
- `desktop/src/smoke_mode.cpp`
- `desktop/src/tray_application.cpp`
- `desktop/tests/platform_test.cpp`
- `desktop/tests/CMakeLists.txt`

**Required contract:**

- Shared Qt components call a platform adapter and do not contain scattered `.exe`, Registry, or `CREATE_NO_WINDOW` assumptions.
- Windows behavior remains unchanged.
- Non-Windows adapters compile and report unsupported features explicitly; they do not claim complete desktop support.

**Non-goals:** No macOS/Linux autostart implementation, packaging, signing, UI redesign, or Core config editor.

**Acceptance:**

- Windows Qt tests remain green.
- Windows, Linux, and macOS CMake configure/build checks are represented in CI.
- Platform hardcoding is confined to the adapter implementation and packaging files.

**Verification:**

```text
cmake -S desktop -B desktop/build -DCMAKE_PREFIX_PATH="$env:QT_ROOT" -DBUILD_TESTING=ON
cmake --build desktop/build --config Release
ctest --test-dir desktop/build -C Release --output-on-failure
git diff --check
```

## T7: Core Configuration Commands

**Goal:** Provide secret-safe Core commands for Qt configuration read, validation, and atomic apply.

**Dependencies:** T5.

**Write scope:**

- `cmd/onellm-router/config_get.go`
- `cmd/onellm-router/config_get_test.go`
- `cmd/onellm-router/config_validate.go`
- `cmd/onellm-router/config_validate_test.go`
- `cmd/onellm-router/config_apply.go`
- `cmd/onellm-router/config_apply_test.go`
- `cmd/onellm-router/main.go`
- `internal/config/config.go`
- `internal/config/config_test.go`

**Required contract:**

- `config-get --json` returns complete non-secret data and `api_key_set`, never the stored key.
- `config-validate --json` returns stable field-level validation errors without writing.
- `config-apply --stdin-json` validates, creates a backup, and atomically replaces the selected YAML.
- Unknown fields and existing comments survive supported-field edits.
- An omitted API key preserves the old key; an explicit replacement is accepted but never echoed or logged.

**Non-goals:** No hot reload, Qt UI, credential vault, or modification of an external attached Core.

**Acceptance:**

- Failure at parse, validation, backup, or replace leaves the original config unchanged.
- Secret-leak tests cover stdout, stderr, logs, backups, and validation errors.
- Existing configuration loading and validation tests remain green.

**Verification:**

```text
go test -count=1 ./internal/config ./cmd/onellm-router
go test -race -count=1 ./internal/config ./cmd/onellm-router
git diff --check
```

## T8: Qt Providers And Models Configuration

**Goal:** Add a Qt main window for Provider and model configuration using only the Core configuration commands.

**Dependencies:** T6 and T7.

**Write scope:**

- `desktop/CMakeLists.txt`
- `desktop/src/config`
- `desktop/src/main_window.cpp`
- `desktop/src/main_window.h`
- `desktop/src/tray_application.cpp`
- `desktop/src/tray_application.h`
- `desktop/tests/config_client_test.cpp`
- `desktop/tests/main_window_test.cpp`
- `desktop/tests/CMakeLists.txt`

**Required contract:**

- Providers expose name, prefix, three Base URLs, API-key-set state, proxy policy, and connection test.
- Models support manual add/remove and protocol-appropriate `/models` discovery.
- Codex reasoning settings and Claude model slots remain editable without inventing new config fields.
- Qt never parses or writes YAML directly.
- External attached Core mode is read-only.

**Non-goals:** No hot reload, MCP/Skill/Prompt pages, Auto Failover, or complete non-Windows desktop lifecycle.

**Acceptance:**

- A temporary-config end-to-end test writes a Provider/model through Core commands and reloads it successfully.
- API keys never appear in Qt model data, logs, error messages, or snapshots.
- Validation errors map to the correct field and do not close the editor or damage the config.

**Verification:**

```text
cmake --build desktop/build --config Release
ctest --test-dir desktop/build -C Release --output-on-failure
git diff --check
```

## T9: Qt Usage Page

**Goal:** Display daily, weekly, and monthly Provider/model Usage from the Core stats JSON contract.

**Dependencies:** T4 and T6.

**Write scope:**

- `desktop/CMakeLists.txt`
- `desktop/src/usage`
- `desktop/src/main_window.cpp`
- `desktop/src/main_window.h`
- `desktop/tests/usage_model_test.cpp`
- `desktop/tests/usage_page_test.cpp`
- `desktop/tests/CMakeLists.txt`

**Required contract:**

- Day/week/month controls, Provider/model filters, token categories, request outcomes, and Retry counts.
- Unknown values are visibly distinct from zero.
- All-attempt Usage and successful-request Usage are visibly distinct.
- UI values come from the Core stats JSON; Qt does not aggregate JSONL independently.

**Non-goals:** No cost estimation, remote sync, charts requiring a new third-party framework, or Usage deletion.

**Acceptance:**

- A shared fixture produces identical CLI JSON and Qt model values.
- Empty, partial, unknown, and malformed-source states are covered without layout instability.
- Existing tray and process-ownership tests remain green.

**Verification:**

```text
cmake --build desktop/build --config Release
ctest --test-dir desktop/build -C Release --output-on-failure
git diff --check
```

## T10: Retry And Release Regression

**Goal:** Complete v1.5.0 integration verification without expanding Retry behavior.

**Dependencies:** T1 through T9.

**Write scope:**

- `internal/upstream/retry_test.go`
- `internal/proxy/proxy_test.go`
- `internal/proxy/proxy_openai_test.go`
- `internal/proxy/proxy_responses_test.go`
- `.github/workflows/release.yml`
- `build.ps1`
- `README.md`
- `README.en.md`
- `CHANGELOG.md`
- `CHANGELOG.zh-CN.md`

**Required contract:**

- Preserve the approved same-Provider retry defaults and stream replay boundary.
- Verify Usage attempt linkage under success, exhausted retry, cancellation, and service shutdown.
- Run the full Go, race, vet, Qt, build-safety, and release-contract gates.
- Document only behavior that is actually shipped.

**Non-goals:** No Retry parameter changes without evidence, Auto Failover, new platform installer, or unrelated cleanup.

**Acceptance:**

- All required gates pass from a clean checkout.
- README and Changelog match the shipped CLI/UI and cross-platform capability matrix.
- A source/config review confirms MCP/Skill/Prompt management and Auto Failover were not introduced.

**Verification:**

```text
go test -count=1 ./...
go test -race -count=1 ./...
go vet ./...
pwsh -NoProfile -File ./build.ps1 -TestOnly
cmake --build desktop/build --config Release
ctest --test-dir desktop/build -C Release --output-on-failure
git diff --check
```
